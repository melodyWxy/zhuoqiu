---
date: 2026-05-08
version: v2.0.0-alpha.1
title: 二期批次1 · 服务端 MVP 可跑
---

# 二期批次 1 · 服务端 MVP 可跑

## 动机

二期拆三批次：批次 1（共享比赛）/ 批次 2（C 端）/ 批次 3（球房赛事）。本轮落地批次 1 的**服务端基础 + 管理端核心 API**，能启动、能登录、能查询，为后续填充业务 API 打底。

## PRD / 设计变化

- `ux/v2/admin-shared-match.md`（新增）：管理端 UI 设计稿
- `ux/v2/shared-match-backend.md`（新增）：服务端架构 + 数据模型 + API + WS 协议；数据库选型 **PostgreSQL**
- `prd/billiards-match-app-prd-v2.md` §6.4：账号系统增加抖音登录 + 跨平台合并规则；§1 范围同步
- 用户确认：
  - 后台范围：全量运营+配置
  - 后台账号：独立体系 + 3 角色 RBAC
  - 技术栈：前端 React + AntD Pro；后端 Node + NestJS
  - 数据库：PostgreSQL
  - 直接进入开发，"跑起来"即可不用审核

## 代码变化

### 新增文件

- **Git**：
  - 仓库初始化（`feat/v1` + `v2` 两分支）
  - `.gitignore`（忽略 node_modules / dist / .taro / .env / .gstack / 本地 claude 配置）
  - Git local user：melodyWxy / 18210711176@163.com

- **服务端 (`server/`)**：
  - `package.json`（NestJS 10 + Prisma 5 + JWT + bcrypt + class-validator + dotenv）
  - `tsconfig.json`、`nest-cli.json`、`.env.example`、`.env`、`README.md`
  - `prisma/schema.prisma`（完整数据模型）+ 首次迁移 `20260508124244_init`
  - `prisma/seed.ts`（super_admin + 系统默认设置）
  - `src/main.ts`、`src/app.module.ts`
  - `src/config/configuration.ts`
  - `src/prisma/prisma.service.ts` + `prisma.module.ts`（全局）
  - `src/common/interceptors/response.interceptor.ts`（统一 `{code,data,traceId}`）
  - `src/common/filters/all-exceptions.filter.ts`（业务异常 → body.code，未知异常 → 500）
  - `src/common/exceptions/business.exception.ts`（+ 错误码常量表）
  - `src/common/dto/pagination.dto.ts`
  - `src/health/health.controller.ts`（+ module）
  - `src/auth/`：`auth.service.ts`、`admin-auth.controller.ts`、`admin-auth.guard.ts`、`roles.decorator.ts`、`current-admin.decorator.ts`、`jwt-payload.ts`、`dto/login.dto.ts`、`auth.module.ts`
  - `src/admin/`：`matches-admin.controller.ts`（列表/详情/事件）、`users-admin.controller.ts`（列表/详情）、`settings-admin.controller.ts`（GET/PATCH）、`analytics-admin.controller.ts`（overview）、`admin.module.ts`

### 数据库

- 在本地 PG 里创建 `zhuoqiu_dev` 库
- 迁移创建 10 张表 + 9 个枚举
- 种子：1 个 super_admin + 8 条默认 system_settings

## 验证步骤

- [x] `npm install`（345 个包）
- [x] `npx prisma migrate dev --name init` 成功
- [x] `npx ts-node prisma/seed.ts` 成功（super_admin + settings）
- [x] `npx nest build` 通过，产物 `dist/main.js`
- [x] `npm run start:prod` 启动 → `服务端启动成功 → http://localhost:3001/v1`
- [x] 冒烟测试（`curl`）：
  - GET `/v1/health` → `{code:0, data:{status:"ok", db:"up"}}`
  - GET `/v1/admin/matches` 无 token → `{code:10002, message:"缺少 Authorization 头"}`
  - POST `/v1/admin/auth/login` 正确密码 → 返回 access+refresh token
  - POST `/v1/admin/auth/login` 错误密码 → `{code:20001}`
  - GET `/v1/admin/matches` 带 token → `{items:[], total:0}`
  - GET `/v1/admin/users` 带 token → `{items:[], total:0}`
  - GET `/v1/admin/settings` 带 token → 8 条默认值
  - GET `/v1/admin/analytics/overview` 带 token → 0 指标

## 遗留问题 / 已知限制

- C 端 API（登录 / 房间 / 事件 / 结束）未实现
- WebSocket（Realtime Hub）未引入
- 管理端写操作（强制结束、封禁、合并）未实现
- 审计日志查询接口、账号管理 CRUD 未实现
- 数据看板只有 overview，其他指标未做
- Redis 未引入（单实例 MVP 不需要）
- Refresh token rotation / 吊销表未实现（MVP 简化）
- 无 CI / 无 Dockerfile / 无测试

## 下一步

按批次 1 继续：

1. C 端 Auth（微信 / 抖音 / 手机号登录，需要 appId/appSecret）
2. C 端 Match API（创建 / 加入 / 事件 / 结束）
3. WebSocket 协议实现
4. 管理端写操作 + 审计
5. 完整数据看板

批次 1 闭环后进入批次 2：管理端 UI（React + AntD Pro）联调。
